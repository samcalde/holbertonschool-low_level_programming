#include "main.h"
#include <stdio.h>
/**
 * _abs - prints absolute value
 * @n: number to be converted
 *
 * Return: absolute value of number.
 */
int _abs(int n)
{
int r;
if (n < 0)
{
r = (-n);
}
return (r);
}
