#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
int a;
for (a = 0; a < 11; a++)
{
printf("%d", a);
}
putchar('\n');
return (0);
}
